<header>
    <nav>
        <ul>
            <li><a href="/">Página de inicio</a>
            {# path (urlr tambien se puede usar genera la ruta absoluta) nos va a generar la ruta desde
            DefaultController, @Route (/default"...) #}
            <li><a href="{{ path('default_index') }}">Empleados HTML</a>
            <li><a href="{{ path('default_index_json', { _format: 'json' }) }}">Empleados JSON</a>
        </ul>
    </nav>
</header> 